{{radio-button
    radioClass=radioClass
    radioId=radioId
    changed=(action "innerRadioChanged")
    disabled=disabled
    groupValue=groupValue
    name=name
    required=required
    value=value}}

{{yield}}
