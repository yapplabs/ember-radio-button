{{yield
    (hash
      radio-button=(
        component 'radio-button'
          groupValue=groupValue
          required=required
          disabled=disabled
          radioClass=radioClass
          changed=changed
      )
    )}}
